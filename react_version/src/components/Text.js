import React from 'react';

const Text = () => {
  return (
    <div>
      <p>
        I'm centered vertically on desktop, but flush with a right edge of some
        sort
      </p>
    </div>
  );
};

export default Text;
